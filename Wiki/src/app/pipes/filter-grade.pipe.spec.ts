import { FilterGradePipe } from './filter-grade.pipe';

describe('FilterGradePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterGradePipe();
    expect(pipe).toBeTruthy();
  });
});
