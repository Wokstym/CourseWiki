import { FilterSemesterPipe } from './filter-semester.pipe';

describe('FilterSemesterPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSemesterPipe();
    expect(pipe).toBeTruthy();
  });
});
